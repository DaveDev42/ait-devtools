node_modules/
dist/
e2e/fixture/dist/
*.tgz
*.tsbuildinfo
.DS_Store
.playwright-mcp/
test-results/
playwright-report/
.tmp/
# Claude Code
.claude/settings.local.json
.claude/*.lock
.claude.lock
.claude/worktrees/
# Machine-local MCP config (points at a local @ait-co/debugger build; never
# committed). Not needed for normal use — agent-plugin's manifest already boots
# the debugger daemon; this file only exists to override that command locally.
.mcp.json
.codegraph/
# Relay TOTP secret (auto-minted 0600 by the env-2 unplugin; a credential that
# gates relay attach — never committed; SECRET-HANDLING)
.ait_relay
